# High-Reliability Trading System Dependencies

# Core dependencies
requests>=2.25.1
python-dotenv>=0.19.0
tenacity>=8.0.1
numpy>=1.20.0
pandas>=1.3.0
aiohttp>=3.8.1
websocket-client>=1.2.1

# Async support
asyncio>=3.4.3
uvloop>=0.16.0  # High-performance event loop for asyncio
aiodns>=3.0.0   # Fast DNS resolver for asyncio
aiohttp-cors>=0.7.0  # CORS support for aiohttp

# Connection pooling
aiosqlite>=0.17.0   # Async SQLite for local data storage
asyncpg>=0.25.0     # High-performance PostgreSQL client for production
redis>=4.2.0        # For distributed connection pooling and rate limiting
aioredis>=2.0.1     # Async Redis client

# Circuit breaker and resilience patterns
pybreaker>=0.6.0    # Python circuit breaker implementation
backoff>=2.1.2      # Exponential backoff decorators
retry>=0.9.2        # Retry decorator for resilient functions
cachetools>=5.0.0   # In-memory caching utilities

# Message queue (for production deployment)
pika>=1.2.0         # RabbitMQ client
aio-pika>=9.0.0     # Async RabbitMQ client
kafka-python>=2.0.2 # Kafka client for high-volume systems
aiokafka>=0.7.2     # Async Kafka client

# Monitoring, alerting and observability
prometheus-client>=0.14.1  # Metrics collection
statsd>=4.0.1              # StatsD client for metrics
datadog>=0.44.0            # DataDog integration
sentry-sdk>=1.5.0          # Error tracking and reporting
python-telegram-bot>=13.11 # Telegram alerts
sendgrid>=6.9.0            # Email alerts

# Logging and tracing
structlog>=21.5.0     # Structured logging
python-json-logger>=2.0.0  # JSON log formatter
opentelemetry-api>=1.9.0   # Distributed tracing
opentelemetry-sdk>=1.9.0   # Distributed tracing

# Web dashboard
fastapi>=0.85.0        # Fast API server for dashboard
jinja2>=3.1.0          # Template engine
uvicorn>=0.18.0        # ASGI server
starlette>=0.19.0      # ASGI toolkit
aiofiles>=0.8.0        # Async file operations for web server

# Testing and development
pytest>=6.2.5
pytest-mock>=3.6.1
pytest-asyncio>=0.18.0  # Async test support
pytest-cov>=3.0.0       # Test coverage
responses>=0.13.0       # Mock HTTP responses
freezegun>=1.2.0        # Time freezing for tests
aioresponses>=0.7.3     # Mock async HTTP responses

# Security and encryption
cryptography>=35.0.0   # Crypto operations
pyjwt>=2.3.0           # JWT for API authentication
passlib>=1.7.4         # Password hashing

# Development tools
black>=22.1.0          # Code formatting
mypy>=0.931            # Type checking
flake8>=4.0.0          # Linting
isort>=5.10.0          # Import sorting
pre-commit>=2.17.0     # Pre-commit hooks
coverage>=6.0.0        # Test coverage

# Utilities
pyyaml>=6.0            # YAML parsing
ujson>=5.0.0           # Fast JSON parsing
tabulate>=0.8.9        # Table formatting
tqdm>=4.62.0           # Progress bars
colorama>=0.4.4        # Terminal colors
schedule>=1.1.0        # Job scheduling
ccxt>=1.60.0           # Crypto exchange APIs 